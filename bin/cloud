#!/usr/bin/env ruby

$LOAD_PATH.unshift(File.dirname(__FILE__) + '/../lib')

require 'cloud'
require 'cloud/cli'

args = ARGV.dup

begin
  EY::Cloud::CLI.command_to_class(args.shift).run(args)
rescue EY::Cloud::CLI::CommandNotFound
  EY::Cloud::CLI.usage
  exit(1)
end
